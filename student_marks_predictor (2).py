# -*- coding: utf-8 -*-
"""STUDENT_MARKS_PREDICTOR.ipynb

Automatically generated by Colab.

Original file is located at
    https://colab.research.google.com/drive/1cG8fXtvv0ngY5W9TTsJkbAcuhbwZkrhn

# 🎯 Student Score Prediction using Machine Learning

This project predicts student marks based on study hours using a regression model.

```

BUSINESS PROBLEM
"""

#IMPORT LIBRARIES
import numpy as np
import pandas as pd
import matplotlib.pyplot as plt

"""LOAD DATASET"""

# 📌 Loading dataset

path=r"/content/student_knn_dataset (1).csv"
df=pd.read_csv(path)

# Display first and last rows
df.head()

df.tail()# it shows the last rows of the datasets

df.shape#there are 10 rows and 2 columns

"""DISCOVER AND VISUALISE THE DATA TO GAIN INSIGHTS"""

df.info()#it gives us the data sets info as it suggests

df.describe()#here it shows the number of students
             #25%of the students study for 3 hours
             #and min max etc as shown below

plt.scatter(x=df["Study_Hours"], y=df["Student marks"])
plt.xlabel("Study Hours")
plt.ylabel("Student Marks")
plt.title("Scatter plot of Study Hours vs Student Marks")
plt.show()

"""PREAPARE THE DATA FOR MACHINE LEARNING ALGORITHMS"""

#DATA CLEANING

df.isnull().sum()#

df.mean()

#split data
x=df.drop("Student marks",axis ="columns")
y=df.drop("Study_Hours",axis="columns")
print("shape  of x =", x.shape)
print("shape of y=", y.shape)

#TRAINIG OF THE DATA
from sklearn.model_selection import train_test_split

x_train,x_test,y_train,y_test =train_test_split(x,y,test_size=0.2,random_state=51)
print("shape  of x_train =", x.shape)
print("shape of y_train=", y.shape)

print("shape  of x_test =", x.shape)
print("shape of y_test=", y.shape)

"""SELECT A MODEL AND TRAIN IT"""

# y = m * x + c
from sklearn.linear_model import LinearRegression
lr=LinearRegression()

lr.fit(x_train,y_train)

lr.coef_

lr.intercept_

m=6.19
c=28.73
y = m * 5 + c

y

lr.predict([[5]])[0][0].round(2)

y_pred =lr.predict(x_test)

y_pred

pd.DataFrame (np.c_[x_test, y_test,y_pred],columns=["Study_Hours","Student_marks_originl","Student_marks_predicted"])

"""FINE-TUNE YOUR MOEL"""

lr.score(x_test,y_test)#itke accuracy ahe

plt.scatter(x_test,y_test)
plt.plot(x_train,lr.predict(x_train),color="r")

"""PRESENT YOUR SOLUTION

AVE ML MODEL
"""

import joblib
joblib.dump(lr,"Student_marks_predictor_model.pkl")

model=joblib.load("Student_marks_predictor_model.pkl")

model.predict([[5]])[0][0]

"""LAUNCH,MONITOR AND MAINTAIN YOUR SYSTEM"""

# ✅ LAUNCH - Predict directly
hours = float(input("Enter study hours: "))
prediction = lr.predict([[hours]])[0][0].round(2)
print(f"Predicted Marks for {hours} study hours: {prediction}")

